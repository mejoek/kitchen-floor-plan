services:
  kitchen-planner:
    build: .
    ports:
      - "8000:8000"
    volumes:
      # Mount the current directory to persist YAML files
      - .:/app
    restart: unless-stopped
    container_name: kitchen-floor-plan
